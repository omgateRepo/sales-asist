{
  "name": "backend",
  "version": "1.0.0",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "dev": "npm run prisma:prepare && nodemon src/index.js",
    "start": "prisma migrate deploy && node scripts/seed.js && node src/index.js",
    "typecheck": "tsc --project tsconfig.json",
    "lint": "eslint .",
    "test": "vitest run",
    "prisma:generate": "prisma generate",
    "prisma:prepare": "prisma migrate deploy && prisma generate",
    "postinstall": "npm run prisma:generate",
    "prisma:migrate:dev": "prisma migrate dev",
    "prisma:migrate:deploy": "prisma migrate deploy",
    "prisma:studio": "prisma studio",
    "seed": "node scripts/seed.js"
  },
  "dependencies": {
    "@ds-proforma/types": "file:../packages/types",
    "@prisma/adapter-pg": "^7.0.1",
    "@prisma/client": "^7.0.1",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "express-rate-limit": "^7.4.0",
    "helmet": "^7.2.0",
    "morgan": "^1.10.0",
    "pg": "^8.16.3",
    "prisma": "^7.0.1"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.1",
    "@types/node": "^22.10.1",
    "eslint": "^9.39.1",
    "globals": "^16.5.0",
    "nodemon": "^3.1.11",
    "typescript": "^5.7.3",
    "vitest": "^2.1.5"
  }
}
