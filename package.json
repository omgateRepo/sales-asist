{
  "name": "sales-asist",
  "private": true,
  "workspaces": ["frontend", "backend", "packages/types"],
  "scripts": {
    "postinstall": "npm --workspace frontend run postinstall",
    "lint": "npm run lint --workspace frontend && npm run lint --workspace backend",
    "typecheck": "npm run typecheck --workspace frontend && npm run typecheck --workspace backend",
    "test": "npm run test --workspace frontend && npm run test --workspace backend",
    "build:api": "npm install && cd backend && npx prisma generate",
    "start:api": "cd backend && npm start",
    "migrate:api": "cd backend && npx prisma migrate deploy",
    "seed:api": "cd backend && npm run seed"
  }
}
